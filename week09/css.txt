layout

  clear
  clip
  direction // 规定文本的书写方向
  display
  float
  height
  flex-basis
  flex-grow
  flex-shrink
  flex-direction
  flex-wrap
  order
  justify-items
  justify-self
  justify-content
  align-content
  align-items
  align-self
  margin-bottom
  margin-left
  margin-right
  margin-top
  overflow-anchor
  overflow-wrap
  overflow-x
  overflow-y
  position
  table-layout
  vertical-align
  grid-auto-columns
  grid-auto-flow
  grid-auto-rows
  grid-column-end
  grid-column-start
  grid-template-areas
  grid-template-columns
  grid-template-rows
  grid-row-end
  grid-row-start
  row-gap
  column-count
  column-gap
  column-rule-color
  column-rule-style
  column-rule-width
  column-span
  column-width


render/draw

  max-height
  max-width
  min-height
  min-width
  padding-bottom
  padding-left
  padding-right
  padding-top
  height
  width
  bottom
  caption-side // 表头位置
  clip
  clip-path
  clip-rule
  line-height
  top
  bottom
  right
  left
  tab-size
  border-bottom-color
  border-bottom-left-radius
  border-bottom-right-radius
  border-bottom-style
  border-bottom-width
  border-collapse
  border-image-outset
  border-image-repeat
  border-image-slice
  border-image-source
  border-image-width
  border-left-color
  border-left-style
  border-left-width
  border-right-color
  border-right-style
  border-right-width
  border-top-color
  border-top-left-radius
  border-top-right-radius
  border-top-style
  border-top-width
  background-attachment
  background-blend-mode
  background-clip
  background-color
  background-image
  background-origin
  background-position
  background-repeat
  background-size
  box-shadow
  box-sizing
  break-after
  break-before
  break-inside
  color
  empty-cells // table 中空单元格渲染方式
  font-family
  font-kerning
  font-optical-sizing
  font-size
  font-stretch
  font-style
  font-variant
  font-variant-ligatures
  font-variant-caps
  font-variant-numeric
  font-variant-east-asian
  font-weight
  isolation // 属性定义该元素是否必须创建一个新的层叠上下文
  letter-spacing
  list-style-image
  list-style-position
  list-style-type
  mix-blend-mode
  object-fit // 规定可替换元素如何适应其内容框
  object-position // 规定可替换元素在其内容框中的位置
  offset-distance // 指定元素的偏移位置
  offset-path // 指定元素的偏移路径
  offset-rotate // 元素在 offset-path 上的方向
  opacity // 设置或返回一个元素必须在页面底部的可见行的最小数量（用于打印或打印预览）
  text-align
  text-align-last
  text-decoration
  text-decoration-line
  text-decoration-style
  text-decoration-color
  text-decoration-skip-ink
  text-underline-position
  text-indent
  text-rendering
  text-shadow
  text-size-adjust
  text-overflow
  text-transform
  unicode-bidi // The unicode-bidi attribute specifies how the accumulation of the background image is managed.
  visibility
  white-space
  word-break
  word-spacing
  z-index
  zoom
  backface-visibility // 指定当元素背面朝向观察者时是否可见
  backdrop-filter // 可以让你为一个元素后面区域添加图形效果
  perspective // 指定了观察者与 z=0 平面的距离，使具有三维位置变换的元素产生透视效果
  perspective-origin // CSS 属性 perspective-origin 指定了观察者的位置perspective 属性的消失点
  shape-outside
  shape-image-threshold
  shape-margin
  filter
  paint-order // 属性可以让你控制文本区域和图形绘制的填充和绘制（和markers）的顺序
  line-break // 可以用来处理如何断开（break lines）带有标点符号的中文、日文或韩文（CJK）文本的行




interactive

  cursor
  pointer-events // 指定在什么情况下 (如果有) 某个特定的图形元素可以成为鼠标事件的 target
  resize // 控制元素可调整大小性
  scroll-behavior // 一个滚动框指定滚动行为，用户行为产生的滚动不受其影响
  speak
  touch-action // 设置触摸屏用户如何操纵元素的区域
  user-select // 控制用户能否选中文本
  caret-color // 定义插入光标（caret）的颜色

svg

  image-rendering
  buffered-rendering
  mask
  color-interpolation
  color-interpolation-filters
  color-rendering
  flood-color
  flood-opacity
  lighting-color
  stop-color
  stop-opacity
  fill
  fill-opacity
  fill-rule
  marker-end
  marker-mid
  marker-start
  mask-type
  shape-rendering
  stroke
  stroke-dasharray
  stroke-dashoffset
  stroke-linecap
  stroke-linejoin
  stroke-miterlimit
  stroke-opacity
  stroke-width
  alignment-baseline
  baseline-shift
  dominant-baseline
  text-anchor
  writing-mode
  vector-effect
  d
  cx
  cy
  x
  y
  r
  rx
  ry

other

  content
  animation-delay
  animation-direction
  animation-duration
  animation-fill-mode
  animation-iteration-count
  animation-name
  animation-play-state
  animation-timing-function
  transform
  transform-origin
  transform-style
  transition-delay
  transition-duration
  transition-property
  transition-timing-function
  will-change
  hyphens // 告知浏览器在换行时如何使用连字符连接单词